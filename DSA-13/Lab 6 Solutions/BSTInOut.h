#include <stdio.h>
#include "BSTOps.h"

extern Book input(FILE *fp, int *size);
extern void InPrint(BST btree, FILE *fp);
extern void InOrderPrint(BookLink x, FILE *fp);
extern void printBook(book s, FILE *fp);
