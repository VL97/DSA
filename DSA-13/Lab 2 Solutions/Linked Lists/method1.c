#include <stdio.h>
#include <stdlib.h>
#include "ListOps.h"

List insert(List list, Link x);

List sort(List list, long long int list_size){
        Node x;
        Link i, j;
        for(j = list -> next; j != NULL; j = j -> next){
                x.data = j -> data;
                i = list;
                
        }
        return list;
}

List insert(){

}
